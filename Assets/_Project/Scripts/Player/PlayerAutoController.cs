using UnityEngine;

namespace IdleSoulSurvivor.Player
{
    /// <summary>
    /// Controls automatic player movement in side-scroller.
    /// Auto: move forward, dodge (% AGI), heal (HP < 30%), pick up loot.
    /// Player only manages: area selection, gear, skill loadout, boss fight.
    /// </summary>
    public class PlayerAutoController : MonoBehaviour
    {
    }
}
