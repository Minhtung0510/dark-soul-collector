using UnityEngine;

namespace DarkSoulCollector.NPC
{
    /// <summary>
    /// Manages dialogue flow: show text, advance, handle choices.
    /// </summary>
    public class DialogueManager : MonoBehaviour
    {
        // TODO: StartDialogue(), AdvanceLine(), EndDialogue()
    }
}
