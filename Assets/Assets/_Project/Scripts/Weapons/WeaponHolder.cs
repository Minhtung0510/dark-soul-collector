using UnityEngine;

namespace DarkSoulCollector.Weapons
{
    /// <summary>
    /// Manages equipped weapon. Handles swap, position, rotation.
    /// </summary>
    public class WeaponHolder : MonoBehaviour
    {
        // TODO: currentWeapon, EquipWeapon(), SwapWeapon()
    }
}
