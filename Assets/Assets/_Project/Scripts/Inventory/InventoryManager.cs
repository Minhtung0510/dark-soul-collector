using UnityEngine;

namespace DarkSoulCollector.Inventory
{
    /// <summary>
    /// Central inventory manager. Add, remove, use, sort items.
    /// </summary>
    public class InventoryManager : MonoBehaviour
    {
        // TODO: items list, maxSlots, AddItem(), RemoveItem(), UseItem()
    }
}
